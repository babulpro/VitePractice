import React from 'react';

const RemoveTask = ({id,data}) => {
    // let filteredArr = {data.filter((v)=>v.id !== id)}
  

    return (
        <div>
            <button onClick={()=>{}}>Remove</button>
        </div>
    );
};

export default RemoveTask;